<div class="section-container">
    <h4 class="section-title"><span>Categories</span></h4>
    <ul class="sidebar-list">
        {% for category in categories %}
            <li><a href="#">{{ category.name }} ({{ category.articles.count() }})</a></li>
        {% endfor %}
    </ul>
</div>